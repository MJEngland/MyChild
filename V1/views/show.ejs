<% include partials/header %>

<h1><%= setting.name %></h1>

<img src="<%= setting.image %>">

<p><%= setting.description %></p>

<% setting.comments.forEach(function(comment){ %>
  <p>
    <strong><% comment.author %></strong> - <% comment.text %>  </p>
<% }) %>
<% include partials/footer %>
